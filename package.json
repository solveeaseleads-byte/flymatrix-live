{
  "name": "flymatrix-backend",
  "version": "1.0.0",
  "description": "FlyMatrix global travel intelligence full-stack engine",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "npm install && npm run build --prefix frontend",
    "generate:guide": "node scripts/generate-guide.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  }
}
